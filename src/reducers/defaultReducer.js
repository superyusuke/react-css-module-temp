const defaultReducer = (state = [], action) => {
  switch (action.type) {
    case 'UPDATE_TEXT':
      return state
    case true:
      return true
    default:
      return state
  }
}

export default defaultReducer